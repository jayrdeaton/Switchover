let actions = require('./actions'),
  colors = require('./colors'),
  helpers = require('./helpers'),
  models = require('./models'),
  program = require('./program');

module.exports = { actions, colors, helpers, models, program };
